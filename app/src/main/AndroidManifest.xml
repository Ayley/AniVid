<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="me.kleidukos.anicloud">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AniCloud">
        <activity
            android:name=".ui.stream.StreamView"
            android:label="AniVid"
            android:theme="@style/Theme.AniCloud.NoActionBar"></activity>
        <activity
            android:name=".ui.videoplayer.StreamPlayer"
            android:screenOrientation="userLandscape"
            android:label="AniVid" />
        <activity
            android:name=".activities.SplashScreenActivity"
            android:label="AniVid"
            android:noHistory="false">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.MainActivity"
            android:label="AniVid">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>